---
topic: W4 — arrangement grows to nine actions
updated: 2026-08-26T21:29
---

- (event) Create. Projection of the W4 corpus at commit d36463a onto this wave. No new content: every claim traces to the PRD, both SRS, both SDD, business-rules, the spine, or DEC-007/008/009.
- (override) Slug is w4-arrangement, one spec for the whole wave. _bmad/custom/bmad-spec.toml states the slug MUST be w<N>-<pc>, one run per wave x Product Component; wdi-build Phase 1 states spec_folder is 'One per wave, not one per wave x component' and Phase 2 says 'One wave = one SPEC'. Followed wdi-build: it owns the wave, it is the caller, and the repo's own history agrees (W1 used w1-verify-0-1-0, W3 used spec-settings-window). Reported as a contradiction in the method package rather than resolved by editing the toml.
- (override) Story Breakdown ran despite headless-mode detection forbidding it. wdi-build Phase 3 dispatches by folder+id and requires stories.yaml to exist, so skipping it would have blocked the pipeline. The six slices were not invented here: they are the ones already recorded in .control/registry/waves.yaml and committed at 21eb0a3 under the owner's instruction.
- (capability) CAP-2 extended (four halves, not two) and CAP-12 born (move to next monitor). Both IDs allocated from .control/registry/requirements.yaml, not from BMad's own sequence, per the bmad-spec customisation.
- (capability) CAP-3 and CAP-5 extended rather than new: the duplicate-chord rule is configuration behaviour and the nine-row pane is settings behaviour. Neither earns a new CAP.
- (constraint) Wire values extended never renumbered; the closed-command-set test replaced not deleted; monitor set never cached; proportional mapping not absolute; maximized window restored before placing; no planner scales by DPI; work area never full monitor bounds; enumeration off the hook thread; no config migration; row order is also precedence order.
- (decision) Stories run serial. Five of six touch daemon/src/hook.rs, so parallel worktrees would collide on the one file every arrangement command passes through. S1 alone first because its shape decision is what the other five build on.
- (decision) done_checkpoint true on every story: this is the first wave to touch the arrangement command set since it was frozen, and each story changes a chord the owner uses daily. spec_checkpoint true on stories 3 and 6 — the two carrying genuinely new surface (Win32 enumeration, and a UI grouping that corrects two drifted spines).
- (assumption) OQ-20 (Ctrl+Alt+Arrow versus graphics-driver rotation), OQ-21 (EnumDisplayMonitors order), and the SetWindowPos virtual-desktop claim carried forward as assumptions. None blocks the wave.
- (event) Self-validate pass 1, coherence: every capability carries intent and success; intents state WHAT; all constraints rule something out; nine non-goals; success signal is a concrete docked-laptop demonstration. Spec Law 1-6 and 8 pass.
- (event) Self-validate pass 2, preservation: walked FR-14, FR-15, FR-22, FR-23, BR-6, LBR-WM-6/7/8, LBR-ST-14, AD-2, AD-9, AD-14, UC-2, UC-4, UC-7, SCN-03, and DEC-007/008/009. Every load-bearing claim landed in SPEC.md or one of the three companions. Wrapper-only content dropped: document status fields, reviewed blocks, revision history rows, and DEC- Alternatives sections (rationale, not contract).

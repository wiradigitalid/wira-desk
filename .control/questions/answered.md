# Answered

**Loaded when:** looking for a decision that was reached through a question, not through a `DEC-`.

Rows move here from the other three lists. MUST NOT be deleted.

## Answered

| id | Question | Answer | Closed |
|---|---|---|---|
| OQ-5 | Does a chord matching a reserved Windows system shortcut need a scenario separate from SCN-01? | No — it is one more rejection class inside the same Listening-state flow, and SCN-01 now carries it explicitly, with three envelope rows and the reason the refusal could not previously reach the screen. The UX the row said had no written authority is now `LBR-ST-10` and `DEC-003`. | 2026-08-26 |
| OQ-9 | Nothing tracked the reserved-shortcut set drifting as Windows adds hotkeys. | Closed by `DEC-003`: the set is a curated catalogue carried as data in `shared`, each entry declaring whether Windows keeps the chord regardless or whether Wira Desk could take it and will not. The drift is now a maintenance surface with an owner rather than an unnoticed silence — and `DEC-003`'s own Cost states that an entry never added is still a chord accepted and then dead, so this closes the tracking gap and not the underlying silence. | 2026-08-26 |

# Assumptions

**Loaded when:** swept once per gate; MAY be skipped.

The **default** class for a question. The agent takes the answer itself and records it here, one
row: the assumption, plus the cost if it turns out wrong. This file **holds nothing**.

A row here MUST move up to `blocking.md` the moment it passes one of the three tests that file
states.

## Open

| id | Assumption | Cost if wrong | Taken | By |
|---|---|---|---|---|
| OQ-1 | UC-6's Failure Flows Step-2 row and the SDD's "Windows Task Scheduler" row are wrong, not SCN-02: `auto_start` stays `true` on Task Scheduler failure and `settings` never calls `schtasks` itself (confirmed: `schtasks`/`std::process::Command` only appear in `crates/daemon/src/autostart.rs` and `legacy.rs`). | Both documents keep contradicting the scenario and code that actually govern this behaviour; an implementer working from UC-6 alone builds a revert path that does not exist. | 2026-08-25 | wdi-review pass (bmad-review, structure lens) |
| OQ-2 | UC-6's Main Flow step ordering (task creation in step 2, before Save in step 3) is wrong; the real order is save → IPC signal → daemon creates/deletes the task, per SRS Constraints, `domain-model.md`, and SCN-02's own Flow. | A reader implementing UC-6 literally has `settings` attempt task creation itself, violating the daemon/settings split `AD-` and the SRS both state. | 2026-08-25 | wdi-review pass |
| OQ-3 | The SDD's IPC status-string quote ("Settings saved. They apply the next time Wira Desk starts.") is wrong; the code's real two strings, "Settings saved and applied" / "Settings saved for next launch" (gated on `reload_signalled`), are what UC-4 already quotes correctly elsewhere in the same corpus. | A reviewer or test written against the SDD's quoted string never matches the real UI text. | 2026-08-25 | wdi-review pass |
| OQ-4 | Multi-way (3+) shortcut collisions, and a Fallback field colliding with its own Primary, are accepted undocumented limitations, not promise gaps: `find_conflict`/`validate_config` only ever name the first colliding pair in field order, and Swap only ever resolves the pair it was invoked on. | A user hits a 3-way collision or a Fallback/Primary collision, sees behaviour no document described, and it gets filed as a bug rather than recognised as a known gap. | 2026-08-25 | wdi-review pass |
| OQ-5 | A chord matching a reserved Windows system shortcut (Win+L/D/Tab/X, Alt+Tab) does not need a scenario separate from SCN-01 — it is one more rejection class inside the same Listening-state flow SCN-01 already governs. | The rejection's actual UX (which field, what message) has no written authority to check a future implementation change against. | 2026-08-25 | wdi-review pass |
| OQ-6 | `state-machines.md`'s missing `Listening → Listening` self-loop (on an invalid chord) and `domain-model.md`'s "reset tutorial from Settings advanced options" transition are different in kind: the self-loop is a real, code-confirmed diagram omission; the "reset tutorial" transition is assumed erroneous or aspirational — `grep` across `crates/settings/src/` found no such control, no FR mentions it, and no UC or scenario owns it. | If "reset tutorial" is actually intended, an unimplemented promise has sat silently in the corpus with nothing tracking it toward being built. | 2026-08-25 | wdi-review pass |
| OQ-7 | UC-6 having no Revert alternate flow, and no scenario for Task Scheduler deletion being denied by policy (distinct from "already removed"), are accepted documentation gaps — inferable for now from UC-4's Revert row and SCN-02's failure pattern rather than written explicitly. | A user unchecks auto-start, is told it succeeded, but a Group Policy block leaves the task in place; the daemon keeps auto-starting at their next login with no warning anywhere in the product. | 2026-08-25 | wdi-review pass |
| OQ-8 | The SDD's Failure Behaviour table not fully corresponding to its own ABCE Boundary Objects list (no row for `eframe` render/graphics-backend init failure, the config-file *read* path, or the single-instance mutex check; no Boundary Object for the two shortcut-validation rows) is filed as a documentation completeness gap, not evidence that any of these paths mishandle failure today. | `Config::load_or_default` silently replacing a corrupted `config.toml` with defaults — wiping a user's shortcuts with no log — would be a real, un-diagnosed failure mode rather than just an undocumented one, and stays invisible until a user reports lost settings. | 2026-08-25 | wdi-review pass |
| OQ-9 | The reserved-shortcut list (`is_reserved_system_shortcut`, 5 combinations) not being flagged as a point-in-time snapshot is a documentation gap, not a functional one — today's SDD correction already states the accept-and-silently-fail behaviour for anything outside that list. | A future Windows version reserves a new chord (e.g. `Win+Z`); nothing in this component tracks that Windows' reserved set can drift, and the same silent failure repeats with no documented awareness it could. | 2026-08-25 | wdi-review pass |
| OQ-10 | The SDD's Decision Summary technical density (Win32 API names, exact RAM/latency figures) is a deliberate, consistent project convention rather than a template violation specific to this document — `window-management`'s SDD's Decision Summary is written in the identical register, so this repo's real SDD audience is engineers, overriding the generic template's "business language for the PO" framing. | Taking the template literally would require a parallel rewrite of both existing SDDs, not just this one — a cross-component cost, which is why this is the one row closest to the blocking bar even though it is filed here. | 2026-08-25 | wdi-review pass |
| OQ-11 | FR-16 (Structured Tray Context Menu) is misattributed to `settings`'s SRS `satisfies:` list — the tray belongs to `window-management` (`crates/daemon/src/menu.rs`), whose own SRS `satisfies:` list does not claim FR-16 either. FR-19 (theming) does legitimately belong to `settings` and is passive behaviour eligible for `no_uc:` rather than missing a UC. | The SRS Gate Checklist's "every FR mapped or justified" claim is unverifiable for FR-16 in either document, and `window-management`'s SRS under-claims what it actually implements. | 2026-08-25 | wdi-review pass |
| OQ-12 | The SDD having no §4 walkthrough for SCN-03 (added today) is an acceptable simplification, not a gap — the Failure Behaviour table's "Shortcut Collision Check" row already carries what a walkthrough would restate. | The SDD's own established one-walkthrough-per-scenario pattern is broken with nothing flagging the inconsistency at the next document review. | 2026-08-25 | wdi-review pass |
| OQ-13 | The `domain-model.md` ↔ `state-machines.md` duplication, and the `domain-model.md`/SRS-Constraints/`rules-settings.md` triple restatement of the same rules, are left as reinforcement rather than merged now — the factual drift inside the first pair is tracked separately at OQ-6. | Independent copies of the same rule or state list keep drifting on future edits — it already happened once: LBR-ST-8/9 landed in `rules-settings.md` today without the SRS body or `domain-model.md` being updated to match. | 2026-08-25 | wdi-review pass |
| OQ-14 | The structure lens's pure editorial trims (UC-4's two grammar-failure rows merged into one; SRS's "To Be Confirmed" boilerplate cut; SDD's redundant Structure bullet cut; SDD's Evidence-table "Verified" repetition trimmed; SDD's chord-ownership paragraph tightened ~27%) are left unapplied — none change meaning, all are word-count or repetition only. | None functionally; the corpus stays more verbose than it needs to until someone applies the reviewer's already-drafted rewrites. | 2026-08-25 | wdi-review pass |
| OQ-15 | 9 further pre-existing prose findings from the same pass are left unapplied — none in content written today, all copy-edit only: SDD Decision Summary misattributes "Task Scheduler auto-start" to `LC-config-writer` (contradicts the Structure table two paragraphs later, which correctly says it only records the preference); two SDD Failure Behaviour "What is logged" cells (OS Theme, Typography) describe an absence of logging without saying so; one SDD Structure bullet and one SDD Decision Summary sentence have modifier-placement ambiguity; SDD's Filesystem Persistence row and three SRS steps (SCN-01 Step 4, SCN-01 Step 6, SCN-02 Step 6) use unattributed passive voice inconsistent with neighboring "System/Settings does X" phrasing; UC-6 Steps 2 and 6 each have one run-on/missing-determiner sentence. | None functionally; ask to expand any one of these nine if it should be applied. | 2026-08-25 | wdi-review pass |

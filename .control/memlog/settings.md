---
topic: settings component depth
artifact: .what/settings/SRS-settings.md
skill: wdi-component intents behaviour + design
updated: 2026-09-03T16:10
---

- (change) behaviour: LBR-ST-14 added — one declared sequence is the single source of the Shortcuts pane draw order, its focus order, and the collision precedence order; grouping under headings permitted but must not reorder. UC-4 gained the Win+Ctrl+Left/Right refusal row from DEC-008 and a precondition naming LBR-ST-14. SRS Constraints gained BR-6 and LBR-ST-14 rows.
- (decision) The nine-field list is written into .how/settings/05-model/data-model.md as the declared sequence, rather than into DESIGN.md. Reason: it is a data ordering that governs collision precedence, not a layout; the grouping that reads it belongs to wdi-ux and is explicitly deferred there.
- (change) Field-count references corrected from six to nine in .how/settings/SDD-settings.md Failure Behaviour and in SCN-03's rationale. LBR-ST list reference in the SRS Slots corrected from LBR-ST-1..9 to 1..14 — it was already stale at 13 before this pass.
- (override) Ordering deviation: this artifact was written while DEC-007, DEC-008, and DEC-009 stood at draft. The owner commissioned the whole batch in one instruction. DEC-006's precedent holds documents until the decision is ratified, and that remains the better order; recorded rather than hidden. All three ratified by the Product Owner in session and raised to applied on 2026-08-26.
- (event) W4 closed 2026-08-26 at release 0.4.0. Six stories landed, 391 to 438 tests. Two method deviations on the owner's explicit instruction: no isolated worktree (worked on main in the shared checkout) and no dispatched workers or code-review panel (the coordinator wrote the code), so cross-model review independence was not obtained. DEC-010 opened and applied mid-wave from a defect found by reading arrangement/win32.rs: the border clamp resolved its monitor from the window, which would have collapsed every monitor-move placement.
- (change) Fixed egui/eframe drift across the settings component's G4 depth: SRS-settings.md (2 constraint/assumption bullets genericized away from naming a specific toolkit, since naming one in .what/ is solution shape), SDD-settings.md (Decision Summary toolkit example, LC table rows, AD-11/AD-11a contract rows re-quoted verbatim from the corrected spine, Failure Behaviour rows for Typography and UI Automation, B-SettingsUI boundary, C-ShellController -- removed a false '500ms repaint polling' claim, Slint is retained-mode and redraws on property change, not immediate-mode -- and 3 Evidence rows), LC-settings-shell.md and LC-shortcut-capturer.md (grounded in the real code: ui/*.slint compiled by slint-build, the daemon-hook-report path via hookbridge.rs drained by a 20ms slint::Timer as the actual source of truth per DEC-004, Slint's key-pressed text as the marked fallback only). Also opened [NEEDS CONFIRMATION] on the settings RAM budget row in the spine (not remeasured since the Slint migration) -- owed to wdi-question before this reopens G4.
- (review by wdi-review) Re-reviewed SRS-settings.md and SDD-settings.md (structure + prose lenses — re-review baseline, not first-review/gate-opening; delta since sha d14c964 was the UC Catalogue-to-pointer and Inherited Constraints Quoted-rule-column drop from the 0.5.46 upgrade, a pure form change touching no money/personal-data/irreversible-action/third-party). Zero findings: pointer targets (usecases.yaml rows, ARCHITECTURE-SPINE.md AD-1/5/11/11a/12/13) verified accurate; remaining prose unchanged from prior review. Stamped reviewed: 2026-09-03, sha 1ee9673, lenses [structure, prose].
- (decision by wdi-component) behaviour: landed UC-8 (check for updates from About pane, satisfies FR-25) in usecases.yaml and .what/settings/04-usecases/; removed FR-25's temporary no_uc note. UC-4 Alternate Flows gained the Swap recovery action (app.rs::can_swap/swap_shortcuts) — swaps back only the field the just-captured chord displaced, no-op otherwise, no re-check for a new collision after. New local rules LBR-ST-15 (daemon liveness: refuse-to-open without a running daemon, auto-close once when it exits) and LBR-ST-16 (deterministic suggest_alternative ladder for a refused chord), with a Rationale section for LBR-ST-15. Fixed a stale 'BR-1 through BR-5' reference (now 8). design: SDD-settings.md's C-KeyCheck corrected — DEC-005 promises 4 verdicts, only 3 are ever produced (Intercepted is declared, never constructed); filed OQ-36 asking whether DaemonNotRunning covering two different root causes is accepted or a UX-honesty gap. Failure Behaviour's System Typography Loader row corrected from 2-tier to the real 3-tier font fallback (same fix as AD-11a at the spine). All triggered by wdi-reconcile's code-vs-doc trace (2026-09-03); code is ground truth.
- (review by wdi-review) Re-reviewed SDD-settings.md and SRS-settings.md + UC-4/UC-8 slots (structure + prose, re-review baseline). Zero findings. Stamped reviewed: 2026-09-03, sha 8ec65d7.
- (decision by wdi-question) OQ-36 closed: product owner confirmed KeyCheckVerdict::DaemonNotRunning covering two root causes (daemon absent vs. chord swallowed by another hook) is NOT a deliberate simplification — a real gap. Moved from assumptions.md to answered.md. Logged DEF-4 (status: open, severity: low, root_cause: incomplete implementation, violates: DEC-005): Intercepted is declared, never constructed; fix_direction is to construct it on the daemon_running=true + no-correlation branch instead of reusing DaemonNotRunning. SDD-settings.md's C-KeyCheck bullet updated to point at DEF-4 instead of the now-closed OQ-36. Code fix itself is not in this session's scope (docs/registry only).
- (decision by wdi-component) design: .how/settings/05-model/data-model.md's user-shortcut-preference table gained 7 new rows (`snap_percent_left/right/top/bottom`, `snap_third_left/middle/right`, all `[MISSING]` — FR-26/FR-27), placed **ahead of** `snap_stack` in the declared sequence on purpose — per `DEC-011`, an install still holding the retired stack default must resolve the chord to the new percent-snap row first, the same mechanism `DEC-009` already built. New small entity `arrangement-percentage-preference` added for the 4 percentage values themselves (not chords, so outside the declared sequence and cannot collide). Also cleared 3 stale `[MISSING]` tags on `snap_top`/`snap_bottom`/`move_next_monitor` — the code has carried all three since W4 closed (2026-08-26); the design doc's markers were never cleared. Found while touching this table for the new rows, not a new problem introduced here.
- (change) Field-count references corrected from nine to sixteen: rules-settings.md's LBR-ST-14 rationale (both mentions), SDD-settings.md's Shortcut Collision Check row (marked which 9 are built vs. which 7 are `[MISSING]`), SCN-03-shortcut-collision-refused-at-save.md's Notes. NOT touched: EXPERIENCE.md and DESIGN.md's "Nine rows in three labelled groups" — deciding which card group the 7 new rows join, or whether a new group is needed, is a real UX design decision (`wdi-ux`'s), not a mechanical count substitution. Reported as a gap for wdi-ux, not fixed here.
- (decision by wdi-blueprint) catalog: registered UC-11 (settings, "Turn a shortcut action on or off", satisfies FR-28) in usecases.yaml. Domain model gained a per-action enabled/disabled flag on `user-shortcut-preference` and a new Per-Action Enablement Invariant, via `mattpocock-skills:domain-modeling` — the key finding: `disabled` and `unbound` (DEC-009's collision-derived state) are conceptually distinct (user preference vs. runtime derivation, different owning component) even though both currently collapse to the identical "chord absent from hook registration" wire representation, so the UI must show which one applies rather than merge them. Confirmed disabling never discards the stored chord string, so `validate_shortcut`'s empty-chord rejection stays untouched — a separate boolean flag, not a relaxed string check. Actor Register's Power User row and SRS `satisfies:` frontmatter extended for FR-28; `BR-1..6` → `BR-1..9` checklist reference corrected (BR-7/BR-8 already existed and were never added to this line — pre-existing staleness, fixed while already touching it). New cross-component BR-9 landed in business-rules.md (Enforcement section deliberately written as "not yet built", pointing at the planned settings/daemon split, since this precedes G4/G5).
- (decision by wdi-component) behaviour: landed UC-11 (turn a shortcut action on or off, satisfies FR-28) in .what/settings/04-usecases/. New local rule LBR-ST-17 in rules-settings.md: a disabled action retains its stored chord and is excluded from collision-precedence checking while off; re-enabling restores the same chord with no re-entry. No rationale subsection added, matching LBR-ST-16's precedent (BR-9 already carries the cross-component why). Read crates/settings/src/persistence.rs (validate_config's field-and-duplicate-check loop) to confirm today's collision check has no path for excluding a field yet — that's design's job, not restated here as implementation.
- (decision by wdi-component) design: LC-config-writer.md gained responsibility 6 (persist per-action enabled flag, exclude disabled from find_conflict, [MISSING] FR-28) and save()'s Realizes widened to UC-4, UC-11. data-model.md's user-shortcut-preference table cleared 7 stale [MISSING] markers on snap_percent_*/snap_third_* (SPEC-1 shipped in DEC-012/PR #16, markers were never cleared — found while already touching this table) and gained a new <action>_enabled row, [MISSING], FR-28. SDD-settings.md's realizes: frontmatter corrected to add UC-8 (also stale-missing, unrelated to this pass but adjacent) and UC-11; ABCE intro line updated; new UC-11 Behaviour & Collaborations walkthrough added after UC-6. Re-stamped reviewed: sha to this pass's commit (structure+prose, medium risk_accepted re-review baseline).
- (decision by wdi-systematic-debugging-adjacent finding, self-caught during design review) Real edge-case-hunter catch: shared::Config's structs use container-level #[serde(default)], which fills a missing field from the struct's own hand-written Default impl, not Rust's primitive bool default of false -- so the new <action>_enabled fields are safe from silently disabling every existing install ONLY if their Default arm explicitly sets true. Documented as a MUST in data-model.md's dictionary note, BR-9's Enforcement, LBR-ST-17's rule text, and a new UC-11 Precondition alternate-flow row -- four homes for one requirement so a builder cannot miss it from any entry point.

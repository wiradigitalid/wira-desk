---
topic: window-management component depth
artifact: .what/window-management/SRS-window-management.md
skill: wdi-component intents behaviour + design
updated: 2026-09-03T16:01
---

- (change) behaviour: UC-2 widened to all four halves (satisfies FR-14 + FR-22, title now 'Snap the active window to half the screen'); UC-7 written for the monitor move; SCN-03 written for the duplicate-chord condition; LBR-WM-7 (monitor-move semantics) and LBR-WM-8 (deterministic half division) added; four invariants added to domain-model.md.
- (decision) UC-2 widened rather than a new UC written for vertical halves. Reason: a UC title must be a sentence a user would say, and 'snap to half the screen' is one sentence covering four chords; a second near-identical UC would have duplicated every alternate and failure flow.
- (change) design: AD-14 quoted verbatim into Inherited Constraints; AD-2's quote resynchronised with the amended spine; two Failure Behaviour boundaries added (Monitor Enumeration, Duplicate Chord Configuration); B-DisplaySet and E-MonitorSet added to ABCE; UC-7 and SCN-03 walkthroughs added; data-model gained the monitor-set entity and a dictionary; 06-flows/flow-monitor-move.md written.
- (assumption) Every new claim about code carries [MISSING] or [PARTIAL] because none of it is written yet — this is a design, not an as-built record. No claim was raised to verified.
- (override) Ordering deviation: this artifact was written while DEC-007, DEC-008, and DEC-009 stood at draft. The owner commissioned the whole batch in one instruction. DEC-006's precedent holds documents until the decision is ratified, and that remains the better order; recorded rather than hidden. All three ratified by the Product Owner in session and raised to applied on 2026-08-26.
- (event) W4 closed 2026-08-26 at release 0.4.0. Six stories landed, 391 to 438 tests. Two method deviations on the owner's explicit instruction: no isolated worktree (worked on main in the shared checkout) and no dispatched workers or code-review panel (the coordinator wrote the code), so cross-model review independence was not obtained. DEC-010 opened and applied mid-wave from a defect found by reading arrangement/win32.rs: the border clamp resolved its monitor from the window, which would have collapsed every monitor-move placement.
- (event) Story 4's guard was seen failing before being trusted, per the repo rule: breaking it turned exactly five asserting tests red and restoring it turned them green. The intermediate reading that said 'still red after restore' was wrong — Copy-Item preserved the backup timestamp so cargo served the broken build.
- (change) wdi-decision apply: landed DEC-010's substance (border clamp resolves the destination monitor, not the window's current one, so a cross-DPI monitor-move doesn't get relocated by Windows) into all 5 files its touches: already named but which wdi-reconcile found carried zero trace of it -- LC-arrangement-engine.md, SDD-window-management.md, rules-window-management.md (LBR-WM-7), UC-7-move-window-next-monitor.md (Failure Flows), flow-monitor-move.md. Also landed DEC-006 (arrangement never targets a Wira Desk window) into the 1 of its 6 touches files missing it, UC-2-snap-window-half.md (Alternate Flows). Both decisions' own files were not edited -- their touches: lists were already correct, only the citations in the target documents were missing. Regenerated .control/generated/ via validate.py --generate; no new validator findings.
- (decision by wdi-component) behaviour: rewrote 02-rules/rules-window-management.md to the template shape (id/Rule/Binds/Source/Status), removing raw Win32 mechanism text now duplicated in .how/window-management/04-components/ (already covered there per evidence check). Corrected BR- citations in UC-1, UC-2, UC-3, UC-7: business-rules.md's Source column shows BR-2..5 do not source any FR these UCs satisfy, so removed as wrongly cited (kept BR-1 and BR-6, which SRS-window-management.md's own Constraints already confirm bind this component broadly); added BR-5 to UC-3, which its Source (FR-12) correctly matches and which was previously missing. Triggered by a wdi-reconcile finding (2026-09-03).
- (review by wdi-review) Re-reviewed SRS-window-management.md and SDD-window-management.md (structure + prose lenses — re-review baseline; same class of delta as settings, since sha 79277c2/d14c964). Zero findings: pointer targets (usecases.yaml rows, ARCHITECTURE-SPINE.md AD-1..10/12/14) verified accurate; remaining prose unchanged from prior review. Stamped reviewed: 2026-09-03, sha 1ee9673, lenses [structure, prose].
- (decision by wdi-component) design: landed 5 code-vs-doc fixes from wdi-reconcile's trace. LC-hook-thread.md: added the DEC-004 capture-lease mechanism (WM_APP_HOOK_LEASE inbound, WM_APP_RECORDED_CHORD outbound, lease_holder_alive reaping, OQ-17) — SDD kernel already had it, this slot didn't. LC-worker-thread.md: added MoveToNextMonitor/SnapTop/SnapBottom to the command list (W4 wave gap, same class as LC-hook-thread's), the dual-path SnapMaximize, and the AttachThreadInput activation-retry algorithm (previously described as a single direct SetForegroundWindow call). LC-tray-controller.md: added the update-check menu integration (FR-16) and the 4th 'Now running' toast class. SDD-window-management.md: Open Items corrected from a false 'None... ratified' to [NEEDS CONFIRMATION] on the never-live-tested virtual_desktop.rs COM path (filed OQ-35); AD-2's stale [MISSING] on wire values 6/7/8 resolved (they exist, tested); AD-14's stale [PARTIAL]/[MISSING] on monitor enumeration resolved (spatial.rs::enumerate_monitors is implemented) — same fix applied to LC-arrangement-engine.md's Depends-on, which carried the identical three stale labels (monitor.rs 'not yet written', spatial.rs enumeration 'MISSING', the three Command variants 'planned'). Code is ground truth throughout.
- (review by wdi-review) Re-reviewed SDD-window-management.md + its 4 LC slots (structure + prose, re-review baseline). Zero findings — backtick parity and formatting checked across all changed slot files. Stamped reviewed: 2026-09-03, sha c35aba0.
- (decision by wdi-blueprint, catalog delta) UC-9 (snap to a custom-percentage edge, satisfies FR-26) and UC-10 (snap to a third, satisfies FR-27) added to usecases.yaml and .what/window-management/04-usecases/; both no_uc placeholders on FR-26/FR-27 removed. Actor Register's Power User row and SRS's FR-coverage checklist line updated to include them. G3 reopened over this delta only, per wdi-blueprint Step 8.
- (decision by wdi-component) behaviour: LBR-WM-9 (deterministic, per-edge-independent percentage division) and LBR-WM-10 (deterministic thirds division, remainder to the middle column) added to rules-window-management.md with rationale. domain-model.md's arrangement-command entity description widened to name the two new kinds. UC-9/UC-10 written full shape (Trigger/Precondition/Main Flow/Alternate/Failure/Outcome/Business Rules), mirroring UC-2's shape.
- (decision by wdi-component) design: LC-arrangement-engine.md gained two new responsibilities (percentage edge snap, thirds snap), two new Depends-on entries (7 new `shared::Command` opcodes at wire values 9-15, extending AD-2's u8 enum; `shared::Config`'s new `snapping.percent_*` fields read at plan time rather than carried on the wire, since the ring buffer holds only a bare opcode), and two new Interface methods (`plan_snap_percent`, `plan_snap_third`) — all marked `[MISSING]`, design only. SDD-window-management.md gained: Robustness Analysis header now cites UC-9/UC-10; `C-ArrangementPlanner`'s description widened; `E-HookCommand`'s byte list gained values 9-15 (`[MISSING]`); two new Behaviour & Collaborations walkthroughs (UC-9, UC-10). data-model.md's `code` column and `region` enum gained the same 7 values. Frontmatter `realizes:` corrected from `[UC-1, UC-2, UC-3]` to include the already-shipped UC-7 (a pre-existing gap, fixed while already touching this field) plus the two new UCs.
- (decision by wdi-build) Phase 1 only: opened `SPEC-1` (release 0.2.0, prd wira-desk, size M, fr [FR-26, FR-27], spec_folder `_bmad-output/specs/spec-1-percentage-and-thirds-snap/`, tickets: [] pending Phase 2). Sized M rather than S because S requires no new FR and this spec introduces two (FR-26, FR-27). Found while opening it: `specs.yaml`'s top-level key was still `waves:` (pre-rename), which meant `rows(self.specs, "specs")` read zero specs — every closed W1-W4 wave has been invisible to validate.py/RTM/status generation this whole time, not just to `uc-scheduled`'s skip message. Renamed the key to `specs:` (mechanical container rename; W1-W4's own `id: W<N>` and `epics`/`stories` nesting left untouched as frozen history, per this skill's own Phase 1 rule and `wdi-upgrade`'s earlier finding that this file's re-cut is wdi-build's). Re-ran `validate.py --check`/`--generate`: GREEN, no new findings; RTM/status now show SPEC-1 (0/0 tickets) and W1-W4 (0/0 tickets — their historical ticket counts are not recoverable from the flat `tickets:` shape the generator reads, since they were written in the retired `epics`/`stories` nesting; this is the generator's existing behavior surfacing for the first time, not a regression from this change). Precondition gaps reported, not silently worked around: not in an isolated worktree (shared checkout — same deviation prior waves took on the owner's explicit instruction; not re-confirmed for this spec). Phase 2 (`/to-spec`, then `/to-tickets`) and Phase 3 (`/tdd`, `/implement`) all carry `disable-model-invocation: true` and were not run — they are owner-invoked, or read-and-follow under a `wdi-autopilot` mandate.
- (decision by wdi-build) Phase 2: ran /to-spec then /to-tickets for SPEC-1 directly (Matt Pocock's engineering skills installed locally via `npx skills@latest add mattpocock/skills`, disable-model-invocation stripped from the local to-spec/to-tickets copies only — implement stays locked, this pass is spec/ticket production, not development). SPEC.md and 2 tickets (SPEC-1-01 percentage-snap, SPEC-1-02 thirds, no blocking edge between them) landed; tickets: index in specs.yaml carries planned test names per ticket (not yet written/passing — Step 1 does that). wdi-review (edge-case-hunter, risk_accepted: medium) found 6 real gaps, all resolved in the same pass — explicit 1-99% range + slider control (grounded in the existing stack_width_percent precedent), boundary work-area coverage matching the half-snap planner's existing test suite, explicit no-live-reapply behavior. Stamped spec_reviewed: 2026-09-06, sha 77bd03a, lenses [edge-case-hunter]. Known gap left unresolved and reported rather than forced: uc-scheduled now reports UC-1/UC-2/UC-3/UC-7 (window-management) as unscheduled, because SPEC-1 is the first spec to touch this component under the flat tickets: shape and W1/W4's historical stories are still in the retired epics/stories nesting. Fixing it would mean fabricating ticket files for already-distilled work or fabricating a review trace for a pass that never happened — both rejected as worse than the gap. Left for the owner.
- (decision by wdi-build) Backfilled W1's flat tickets: index (S1, S2, S3 only, satisfying UC-1/UC-2/UC-3) so uc-scheduled can see them as already scheduled, per owner's explicit choice to fix W1 honestly and leave W4/UC-7 as a reported gap. Deliberately excluded W1's settings-side stories (S4-S6): adding them would mark settings touched too and newly redden UC-8 (no spec schedules it yet) — trading one gap for another. Copied (not moved) S1-S3's story files from stories/ to a new issues/ folder so ticket-status-one-home resolves them; stories/ untouched. W1's existing spec_reviewed (2026-08-21, sha 7a0d260, lenses incl. edge-case-hunter) already covered this, no new review needed. validate.py: down to the single known UC-7 finding.
- (review by wdi-review) Real edge-case-hunter pass over W4's contract (SPEC.md + all 6 stories) to legitimately backfill its missing spec-level review trace, per owner's explicit go-ahead to finish the remaining uc-scheduled gap. One genuine finding: UC-7's Alternate/Failure Flows never distinguished monitor-move's "refuse rather than invent a minimum size" behavior (verified in arrangement/monitor.rs: `Rect::new` refuses a share that rounds to zero) from half-snap's "respect the window's own enforced minimum size" — fixed in UC-7-move-window-next-monitor.md (new Failure Flow row) and LC-arrangement-engine.md (new Notes bullet), code being ground truth. Backfilled W4's tickets: index with only W4-S3 (satisfies UC-7) — S1/S4/S5 satisfy no UC, S2's UC-2 is already scheduled via W1-S2, S6 (settings) deliberately excluded for the same reason W1's S4-S6 were. Copied S3's story file into a new issues/ folder (stories/ untouched). validate.py: GREEN, 0 findings.
- (decision by wdi-blueprint) catalog: registered UC-12 (window-management, "A disabled shortcut action's chord is not claimed at the hook", satisfies FR-29) in usecases.yaml. Domain model gained a Registration Exclusion Invariant and a note on `hook-command`'s Lifecycle table (a disabled action's command never reaches `Pending` at all — a registration-time precondition, not a new terminal state) plus a disambiguating clause on the existing One Chord, One Action Invariant, via `mattpocock-skills:domain-modeling`. Code cross-reference against `crates/daemon/src/hook.rs`'s `Chords`/`unbind_duplicates`/`match_shortcut`: both `disabled` and `unbound` already collapse to `Option<Shortcut> == None` at this layer, so no new collision-handling code is needed there — exclusion happens one step earlier, at `Chords` construction. Actor Register's Power User row and SRS `satisfies:` frontmatter extended for FR-26/FR-27 (pre-existing gap, `satisfies:` never carried them despite the body checklist claiming coverage) and FR-29 (new); `BR-1..6` → `BR-1..9` checklist reference corrected for the same pre-existing reason noted in settings.md. `uc-scheduled` now reports UC-12 as an unscheduled gap in this already-touched component — expected until SPEC-2/SPEC-3 schedule it; not worked around.
